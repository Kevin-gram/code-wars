
// const key = 'dynamicKey';
// Write the code to create an object named myObject with:

// A computed property name based on key.
// A method named greet that logs "Hello, World!".
// Answers:
const key = 'dynamicKey';
const myObject={
     key,
    greeting(){
        return`Hello ${name}`
    }
}
console.log(myObject)